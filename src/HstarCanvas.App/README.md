# HstarCanvas.App

WinUI 3 / Windows App SDK application shell placeholder.

This directory is reserved for the native Windows 11 shell that will host WebView2, Mica/Acrylic window materials, single-instance startup, system tray behavior, and installer-aware lifecycle hooks.

Create the WinUI 3 project here after confirming the local Windows App SDK templates are installed.

